# Absolute path whose direct child folders are the manga library.
POCKET_MANGA_EDITOR_WORKING_DIRECTORY=C:\Manga Library

# Listen on every IPv4 network interface so trusted LAN devices can connect.
POCKET_MANGA_EDITOR_HOST=0.0.0.0
POCKET_MANGA_EDITOR_PORT=8765
